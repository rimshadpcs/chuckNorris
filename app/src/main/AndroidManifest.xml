<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rimapps.chucknorris" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChuckNorris" >
        <activity
            android:name=".activities.CategoryActivity"
            android:exported="false"
            android:label="@string/title_activity_category"
            android:theme="@style/Theme.ChuckNorris.NoActionBar" />
        <activity
            android:name=".activities.PopUpActivity"
            android:exported="false"
            android:label="@string/title_activity_pop_up"
            android:theme="@style/Theme.ChuckNorris.NoActionBar" />
        <activity
            android:name=".activities.SearchActivity"
            android:exported="false"
            android:label="@string/title_activity_random_joke"
            android:theme="@style/Theme.ChuckNorris.NoActionBar" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>